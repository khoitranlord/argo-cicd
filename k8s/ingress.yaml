apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simple-test-app-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: simple-test-app.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: simple-test-app-service
            port:
              number: 80